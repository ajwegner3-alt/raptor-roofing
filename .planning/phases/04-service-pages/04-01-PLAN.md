---
phase: 04-service-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/services.ts
  - src/content/faqs.ts
  - src/components/sections/LeadForm.tsx
  - src/components/sections/TestimonialCarousel.tsx
  - src/components/sections/ServiceHero.tsx
  - src/components/sections/ServiceBreadcrumb.tsx
  - src/components/sections/ServiceCTABand.tsx
  - src/components/sections/BeforeAfterGallery.tsx
  - src/components/sections/RelatedServicesBlock.tsx
  - src/components/templates/ServicePageTemplate.tsx
  - src/app/(marketing)/services/roofing/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /services/roofing loads a distinct page with roofing-specific hero, problem checklist, and process steps"
    - "The roofing page renders visible Home / Services / Roofing breadcrumbs above the H1"
    - "The roofing page H1 reads 'Roof Replacement & Repair in Omaha' (or similar '[Service] in Omaha' format) as a single H1"
    - "The roofing page displays dual CTAs (Call Now tel: link + Free Estimate anchor) in the hero"
    - "The roofing page includes an in-page LeadForm with the Service dropdown pre-selected to 'roofing'"
    - "The roofing page internally links to at least 2 sibling services and to the homepage"
    - "The roofing page includes 2-4 visible [PLACEHOLDER] before/after gallery slots"
    - "The roofing page ships Service JSON-LD, BreadcrumbList JSON-LD, and FAQPage JSON-LD"
    - "The roofing page has a unique title tag, meta description, and canonical URL set via buildMetadata({ path: '/services/roofing' })"
  artifacts:
    - path: "src/content/services.ts"
      provides: "problemChecklist: string[] field on Service interface, populated for roofing (and all four services)"
      contains: "problemChecklist"
    - path: "src/content/faqs.ts"
      provides: "At least 3 FAQs tagged with serviceSlug: 'roofing' (combined with existing tagged items reaches 3-5 minimum)"
      contains: "serviceSlug: \"roofing\""
    - path: "src/components/sections/LeadForm.tsx"
      provides: "defaultService?: string prop that initializes the service state"
      contains: "defaultService"
    - path: "src/components/sections/TestimonialCarousel.tsx"
      provides: "optional testimonials?: Testimonial[] prop with fallback to full array"
      contains: "testimonials?"
    - path: "src/components/sections/ServiceHero.tsx"
      provides: "New parameterized hero component accepting a Service prop"
      min_lines: 80
    - path: "src/components/templates/ServicePageTemplate.tsx"
      provides: "Shared 11-section template rendering all service page sections in Problem -> Process -> Proof -> Form order"
      min_lines: 120
    - path: "src/app/(marketing)/services/roofing/page.tsx"
      provides: "Static route file with metadata export, JSON-LD injection, and ServicePageTemplate composition"
      contains: "getServiceBySlug(\"roofing\")"
  key_links:
    - from: "src/app/(marketing)/services/roofing/page.tsx"
      to: "src/components/templates/ServicePageTemplate.tsx"
      via: "import + JSX"
      pattern: "ServicePageTemplate"
    - from: "src/app/(marketing)/services/roofing/page.tsx"
      to: "src/lib/schema.tsx"
      via: "JsonLd + serviceSchema + breadcrumbSchema + faqPageSchema"
      pattern: "serviceSchema\\("
    - from: "src/components/templates/ServicePageTemplate.tsx"
      to: "src/components/sections/LeadForm.tsx"
      via: "defaultService prop"
      pattern: "defaultService="
    - from: "src/components/sections/ServiceHero.tsx"
      to: "Free Estimate anchor"
      via: "href='#estimate-form'"
      pattern: "#estimate-form"
---

<objective>
Build the authoritative service page template and ship `/services/roofing` as the reference implementation. This plan closes the content gaps (problemChecklist, per-service FAQ tagging, LeadForm + TestimonialCarousel props) and creates the shared ServicePageTemplate that plans 04-02 and 04-03 will reuse without modification.

Purpose: Establish a single 11-section template (Breadcrumbs → Hero → Problem → Process → Mid-CTA → TrustStrip → BeforeAfter → Testimonials → FAQ → Related → LeadForm) grounded in research findings. Roofing is built first as the authoritative pattern; siding/gutters/emergency-tarping reuse the same component with copy-only divergence plus the `isEmergency` branch.

Output: Modified content files, modified LeadForm and TestimonialCarousel, new ServiceHero + template + supporting sections, and a fully-functional /services/roofing route with metadata, JSON-LD, and visible breadcrumbs.
</objective>

<execution_context>
@C:\Users\andre\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\andre\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-service-pages/04-CONTEXT.md
@.planning/phases/04-service-pages/04-RESEARCH.md

# Ground-truth source files — read before modifying
@src/content/services.ts
@src/content/faqs.ts
@src/content/testimonials.ts
@src/content/site.ts
@src/components/sections/Hero.tsx
@src/components/sections/LeadForm.tsx
@src/components/sections/TestimonialCarousel.tsx
@src/components/sections/FaqAccordion.tsx
@src/components/layout/TrustStrip.tsx
@src/lib/schema.tsx
@src/lib/metadata.ts
@src/app/(marketing)/page.tsx
@src/app/(marketing)/layout.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close content gaps — services.ts, faqs.ts, LeadForm props, TestimonialCarousel props</name>
  <files>
    src/content/services.ts
    src/content/faqs.ts
    src/components/sections/LeadForm.tsx
    src/components/sections/TestimonialCarousel.tsx
  </files>
  <action>
Fix every content/infrastructure gap surfaced in 04-RESEARCH.md Goal 3 and Goal 4 BEFORE building the template. These are foundational and must all land in this task.

1. **src/content/services.ts** — Add `problemChecklist: string[]` to the `Service` interface and populate it for ALL FOUR services (roofing, siding, gutters, emergency-tarping). Do NOT remove or rename `problemCopy`; it can stay as an intro paragraph.
   - Each checklist is 4-6 symptom-led, homeowner-observable bullets written in second person or declarative ("Dark circular dents or bruising on shingles", "Granules collecting in gutters", "Water stains on attic ceiling", etc.).
   - Roofing checklist MUST be realistic symptom language (hail impact, curling shingles, granules in gutters, daylight in attic, water stains, sagging deck). No fear copy.
   - Siding checklist: cracked/warped panels, fading/chalking, soft spots, loose panels after wind, visible water intrusion behind panels.
   - Gutters checklist: overflowing during rain, sagging sections, visible rust/seam separation, water pooling near foundation, detached downspouts.
   - Emergency-tarping checklist: missing shingles after storm, visible decking, water entering attic/ceiling, tree impact, wind-lifted sections. Keep language calm and factual — NO fear words ("catastrophic", "devastating", "emergency disaster").

2. **src/content/faqs.ts** — Add service-specific FAQs so that `getFaqsByService("roofing" | "siding" | "gutters" | "emergency-tarping")` each returns at least 3 items (target 4). Existing roofing tags (faq-1, faq-3, faq-7, faq-9) already satisfy roofing. You MUST add at minimum:
   - 4 new FAQs with `serviceSlug: "siding"` covering cost range, materials (vinyl/fiber-cement/wood), timeline, warranty. Use `[PLACEHOLDER]` inline for any unknown specifics (exact pricing, warranty years).
   - 4 new FAQs with `serviceSlug: "gutters"` covering sizing (5" vs 6"), guards, seamless vs sectional, timeline.
   - 4 new FAQs with `serviceSlug: "emergency-tarping"` covering response time ("24/7 answer, same-day tarp-ups"), documentation for insurance ("we photograph damage before tarping"), cost during storm events, and how long a tarp holds. NONE of these FAQs may include anti-chaser forbidden strings (no "limited time", no "insurance will pay", no "free roof", no "zero out of pocket", no "act now", no "don't wait"). Re-read 04-RESEARCH.md Goal 8 before writing.
   - Follow the existing FAQ interface shape (id, question, answer, serviceSlug). IDs should be sequential (faq-10, faq-11, ...) — verify by reading the file first.
   - The answers must be realistic Claude-drafted copy, not lorem ipsum. Any price/warranty-length/license-number unknowns wear an inline `[PLACEHOLDER]` tag.

3. **src/components/sections/LeadForm.tsx** — Add ONE optional prop:
   ```typescript
   interface LeadFormProps {
     defaultService?: string;
   }
   export function LeadForm({ defaultService = "" }: LeadFormProps) {
     const [service, setService] = useState(defaultService);
     // ... rest unchanged
   }
   ```
   The `id="estimate-form"` on line 43 MUST stay. Homepage usage `<LeadForm />` must continue to work (defaultService defaults to "").

4. **src/components/sections/TestimonialCarousel.tsx** — Add an optional `testimonials?: Testimonial[]` prop with fallback to the full imported array:
   ```typescript
   interface TestimonialCarouselProps {
     testimonials?: Testimonial[];
   }
   export function TestimonialCarousel({ testimonials: propTestimonials }: TestimonialCarouselProps = {}) {
     const testimonials = propTestimonials ?? allTestimonials; // rename existing import
     // ... rest unchanged
   }
   ```
   Rename the hardcoded `testimonials` import to `allTestimonials` (or similar) to avoid shadowing. Homepage usage `<TestimonialCarousel />` must continue to work.

Do NOT add any other changes to these files. Keep diffs minimal and mechanical.
  </action>
  <verify>
Run from repo root (bash):

```bash
# services.ts gap closed
grep -n "problemChecklist" src/content/services.ts | head -20   # interface + 4 populated entries = 5+ matches

# faqs.ts gap closed
grep -c 'serviceSlug: "roofing"' src/content/faqs.ts            # >= 3
grep -c 'serviceSlug: "siding"' src/content/faqs.ts             # >= 3
grep -c 'serviceSlug: "gutters"' src/content/faqs.ts            # >= 3
grep -c 'serviceSlug: "emergency-tarping"' src/content/faqs.ts  # >= 3

# LeadForm prop
grep -n "defaultService" src/components/sections/LeadForm.tsx   # interface + destructure + useState

# TestimonialCarousel prop
grep -n "testimonials?" src/components/sections/TestimonialCarousel.tsx

# Anti-chaser guardrail smoke-test on the emergency-tarping FAQ block
! grep -i "limited time\|act now\|insurance will pay\|free roof\|zero out of pocket\|don.t wait" src/content/faqs.ts

# Type check must pass
npx tsc --noEmit
```
  </verify>
  <done>
- Service interface has problemChecklist; all 4 services populated with 4-6 symptom bullets each
- faqs.ts has >=3 tagged FAQs for every service slug
- LeadForm accepts defaultService and initializes state from it; homepage still compiles
- TestimonialCarousel accepts optional testimonials prop; homepage still compiles
- No anti-chaser forbidden strings in faqs.ts
- `npx tsc --noEmit` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Build ServiceHero, ServicePageTemplate, and all supporting section components</name>
  <files>
    src/components/sections/ServiceHero.tsx
    src/components/sections/ServiceBreadcrumb.tsx
    src/components/sections/ServiceCTABand.tsx
    src/components/sections/BeforeAfterGallery.tsx
    src/components/sections/RelatedServicesBlock.tsx
    src/components/templates/ServicePageTemplate.tsx
  </files>
  <action>
Create the reusable template and all new section components. These must work for ALL FOUR service pages — 04-02 and 04-03 will not modify any of these files. The only per-page divergence is routed through `service.isEmergency` inside the template.

**1. src/components/sections/ServiceHero.tsx** (NEW)

Server component (no "use client"). Props:
```typescript
interface ServiceHeroProps {
  service: Service;
}
```

Structural requirements:
- Reuse the 5-layer stack pattern from `src/components/sections/Hero.tsx` (read it first): background `next/image` with `fill priority sizes="100vw"`, gradient overlay, subtle noise texture, content layer, roof-peak SVG divider at the bottom.
- Background image = `service.heroImagePath` with alt = `service.heroImageAlt`.
- Left column content:
  - H1 = `service.headline` (format `[Service] in Omaha, NE`). One H1 per page. Use font-display class, large size (`text-4xl lg:text-6xl`).
  - Subheadline from `service.subheadline`.
  - Micro-trust line below subheadline: `"Licensed NE · Bonded & Insured · 15 Years Local"` — single line, small, neutral color. Do NOT reproduce the full BBB/trust-signal badge stack; TrustStrip immediately below the hero carries that weight.
  - Dual CTAs in a flex row:
    1. Primary "Call Now" — `<a href={`tel:${siteConfig.phoneHref}`}>` styled with `bg-accent-600` and same button pattern as homepage. Import `siteConfig` from `@/content/site`.
    2. Secondary "Free Estimate" — `<a href="#estimate-form">` — anchors to the in-page LeadForm.
- Right column:
  - If `!service.isEmergency`: render `<LeadForm defaultService={service.slug} />` (same visual column as homepage).
  - If `service.isEmergency`: render a tap-to-call card (large `tel:` link, 48px+ font — use `text-4xl lg:text-5xl font-display`, tall touch target). The card includes the phone number, a short "We answer 24/7 · Same-day tarp-ups across Omaha" line, and a secondary "Free Estimate" link to `#estimate-form`. The LeadForm is NOT embedded in the hero on emergency pages — it appears later in the template's final LeadForm section.
- Roof-peak SVG divider at the bottom. Fill color must match the next section (TrustStrip = `#f4f6f7`) — copy the divider block from Hero.tsx.
- Use ONLY existing `@theme` tokens (no new hex values).

**2. src/components/sections/ServiceBreadcrumb.tsx** (NEW)

Server component. Props:
```typescript
interface ServiceBreadcrumbProps {
  serviceShortTitle: string;
  serviceSlug: string;
}
```

Renders:
```jsx
<nav aria-label="Breadcrumb" className="bg-neutral-50 border-b border-neutral-200">
  <div className="mx-auto max-w-7xl px-4 py-3 lg:px-8">
    <ol className="flex items-center gap-2 font-body text-sm text-neutral-600">
      <li><Link href="/" className="hover:text-accent-600">Home</Link></li>
      <li aria-hidden="true">/</li>
      <li><Link href="/#services" className="hover:text-accent-600">Services</Link></li>
      <li aria-hidden="true">/</li>
      <li aria-current="page" className="text-primary-900 font-semibold">{serviceShortTitle}</li>
    </ol>
  </div>
</nav>
```

Use `next/link` for internal links. Home and Services must be real anchor tags. The current page item has `aria-current="page"` and NO link.

**3. src/components/sections/ServiceCTABand.tsx** (NEW)

Server component. No props (or a single optional `phoneLabel` prop). Renders a mid-page full-width band with:
- Short H2 or headline ("Ready for a straight answer on your [service]?" — but keep service-neutral since this is shared. Use something like "Talk to an Omaha roofer who calls back." or "Ready for a free estimate? We answer the phone.")
- Dual CTAs: Call Now tel: link + Free Estimate anchor to `#estimate-form`.
- Use primary/accent color palette. No animation, no countdown, no urgency language. No anti-chaser forbidden strings.

**4. src/components/sections/BeforeAfterGallery.tsx** (NEW)

Server component. Props:
```typescript
interface BeforeAfterGalleryProps {
  slotCount?: number; // default 3, valid range 2-4
}
```

Renders a grid of `slotCount` labeled placeholder cards. Each card has:
- A 4:3 aspect ratio placeholder box with `bg-neutral-200`
- An amber banner with the text `[PLACEHOLDER]` (use `bg-amber-100 text-amber-900 border border-amber-300` or existing tokens)
- A small descriptive caption like "Before/After — Omaha project #1". Captions MUST be factual and neutral — NO fear-implying language like "catastrophic damage" or "devastating storm".

The component must render `slotCount` slots; do not hide slots. Captions can be neutral and numbered (Project 1, 2, 3) — no real locations required.

**5. src/components/sections/RelatedServicesBlock.tsx** (NEW)

Server component. Props:
```typescript
interface RelatedServicesBlockProps {
  relatedServices: Service[];
}
```

Renders a 3-card grid (desktop 3 cols, mobile 1 col) where each card links to `/services/${related.slug}` and shows:
- `related.shortTitle` as card heading
- `related.subheadline` or a short blurb
- A small "Learn more →" affordance

All card links must use `next/link`. Cards are clickable (wrap card content in `<Link>` or use an overlay anchor). No dynamic class construction.

**6. src/components/templates/ServicePageTemplate.tsx** (NEW — create the directory)

Server component. Props (exactly as in 04-RESEARCH.md Goal 2):
```typescript
import type { Service } from "@/content/services";
import type { FAQ } from "@/content/faqs";
import type { Testimonial } from "@/content/testimonials";

interface ServicePageTemplateProps {
  service: Service;
  faqs: FAQ[];
  testimonials: Testimonial[];
  relatedServices: Service[];
}
```

Renders the 11 sections in this EXACT scroll order (Problem → Process → Proof → Form):

1. `<ServiceBreadcrumb serviceShortTitle={service.shortTitle} serviceSlug={service.slug} />`
2. `<ServiceHero service={service} />`
3. **Problem section** — H2 like "Is your [service area] showing these signs?". Short intro from `service.problemCopy` (existing prose). Then render `service.problemChecklist` as a checklist `<ul>` with a checkmark icon (lucide-react `Check` or inline SVG) per item. Scannable, not narrative.
4. **Process section** — H2 "How we do it". Render `service.processSteps` as a numbered 3-4 step list with step number, title, and description. Service-specific steps (already in services.ts). Layout: horizontal on desktop (flex row or grid-cols-3/4), stacked on mobile.
5. `<ServiceCTABand />` — mid-page CTA band
6. `<TrustStrip />` — import from `@/components/layout/TrustStrip`, render as-is, no props.
7. `<BeforeAfterGallery slotCount={3} />`
8. `<TestimonialCarousel testimonials={testimonials} />` — scoped subset (or fallback if empty — if testimonials array is empty, you may render the full array by passing no prop, OR render nothing and skip the section. Preferred: if `testimonials.length === 0`, skip rendering the carousel entirely rather than showing an empty widget).
9. `<FaqAccordion faqs={faqs} />` — reuse as-is; already accepts FAQ[].
10. `<RelatedServicesBlock relatedServices={relatedServices} />`
11. `<LeadForm defaultService={service.slug} />` — reuse homepage component with new prop.

**Emergency Tarping conditional sections** — rendered only when `service.isEmergency === true`:
- After the hero (between sections 2 and 3), inject an **Insurance Documentation block**:
  - H2 like "We document damage for your claim before we tarp."
  - 2-3 paragraphs explaining Raptor photographs damage on arrival, times the response, and gives the homeowner the documentation for their insurance adjuster.
  - MUST include the word "document" (hard-gate grep target).
  - MUST include the word "insurance" at least once.
  - MUST NOT include any anti-chaser forbidden strings ("insurance will pay", "free roof", "zero out of pocket", etc.).
  - Tone: calm, capable, factual. NOT fear-inducing.
- No other structural branches are allowed. Do NOT hide any of the 11 standard sections on emergency-tarping.

**Styling rules:**
- Use ONLY existing Tailwind v4 tokens (`bg-primary-*`, `bg-accent-*`, `text-primary-*`, `bg-neutral-*`, `bg-surface`, etc.). No inline hex, no new CSS variables.
- No dynamic class construction.
- Server components only (template, hero, breadcrumb, CTA band, gallery, related block). LeadForm and TestimonialCarousel remain the existing client components.
- Responsive: mobile-first, lg: breakpoint for desktop layouts.

**Do NOT** duplicate `localBusinessSchema()` — that JSON-LD is already mounted in Footer. The template renders JSX only; JSON-LD injection happens at the page level in Task 3.
  </action>
  <verify>
```bash
# New files exist
ls src/components/sections/ServiceHero.tsx
ls src/components/sections/ServiceBreadcrumb.tsx
ls src/components/sections/ServiceCTABand.tsx
ls src/components/sections/BeforeAfterGallery.tsx
ls src/components/sections/RelatedServicesBlock.tsx
ls src/components/templates/ServicePageTemplate.tsx

# ServiceHero uses service prop and has tel:/#estimate-form anchors
grep -l "service.heroImagePath\|service\.heroImagePath" src/components/sections/ServiceHero.tsx
grep "#estimate-form" src/components/sections/ServiceHero.tsx
grep "tel:" src/components/sections/ServiceHero.tsx

# ServiceBreadcrumb has aria-label Breadcrumb and aria-current page
grep 'aria-label="Breadcrumb"' src/components/sections/ServiceBreadcrumb.tsx
grep 'aria-current="page"' src/components/sections/ServiceBreadcrumb.tsx

# Template wires all 11 sections and has the isEmergency branch
grep "ServiceBreadcrumb\|ServiceHero\|TrustStrip\|BeforeAfterGallery\|TestimonialCarousel\|FaqAccordion\|RelatedServicesBlock\|LeadForm\|ServiceCTABand" src/components/templates/ServicePageTemplate.tsx | wc -l   # expect >= 9
grep "isEmergency" src/components/templates/ServicePageTemplate.tsx
grep "defaultService={service.slug}" src/components/templates/ServicePageTemplate.tsx
grep "problemChecklist" src/components/templates/ServicePageTemplate.tsx
grep "processSteps" src/components/templates/ServicePageTemplate.tsx

# No forbidden anti-chaser strings anywhere in the new files
! grep -ri "limited time\|act now\|insurance will pay\|free roof\|zero out of pocket\|don.t wait\|countdown\|expires" src/components/sections/ServiceHero.tsx src/components/sections/ServiceCTABand.tsx src/components/sections/BeforeAfterGallery.tsx src/components/sections/RelatedServicesBlock.tsx src/components/templates/ServicePageTemplate.tsx

# Type check
npx tsc --noEmit
```
  </verify>
  <done>
- All 6 new files exist
- ServicePageTemplate renders all 11 sections in correct order
- isEmergency branch injects the insurance documentation block with "document" and "insurance" strings
- ServiceHero renders dual CTAs (tel: + #estimate-form), parameterized background image, and branches on isEmergency for the right column
- No anti-chaser forbidden strings in any new file
- `npx tsc --noEmit` passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Ship /services/roofing page with metadata and JSON-LD</name>
  <files>
    src/app/(marketing)/services/roofing/page.tsx
  </files>
  <action>
Create the roofing route file that composes the template with all required schema and metadata.

**File:** `src/app/(marketing)/services/roofing/page.tsx`

Exact structure (server component):

```typescript
import { buildMetadata } from "@/lib/metadata";
import { JsonLd, serviceSchema, breadcrumbSchema, faqPageSchema } from "@/lib/schema";
import { getServiceBySlug, getRelatedServices } from "@/content/services";
import { getFaqsByService } from "@/content/faqs";
import { getTestimonialsByService } from "@/content/testimonials";
import { ServicePageTemplate } from "@/components/templates/ServicePageTemplate";

const service = getServiceBySlug("roofing")!;
const faqs = getFaqsByService("roofing");
const relatedServices = getRelatedServices("roofing");

export const metadata = buildMetadata({
  title: service.metadata.title,
  description: service.metadata.description,
  path: "/services/roofing",
  useAbsoluteTitle: true,
});

export default function RoofingPage() {
  const testimonials = getTestimonialsByService("roofing");
  return (
    <>
      <JsonLd data={serviceSchema(service)} />
      <JsonLd data={breadcrumbSchema([
        { name: "Home", href: "/" },
        { name: "Services", href: "/#services" },
        { name: service.shortTitle, href: "/services/roofing" },
      ])} />
      <JsonLd data={faqPageSchema(faqs)} />
      <ServicePageTemplate
        service={service}
        faqs={faqs}
        testimonials={testimonials}
        relatedServices={relatedServices}
      />
    </>
  );
}
```

**Rules:**
- Use static route folder `src/app/(marketing)/services/roofing/page.tsx` — NOT a `[slug]` dynamic route.
- `useAbsoluteTitle: true` is mandatory (prevents double-brand in title).
- Define `faqs` ONCE and pass the same variable to both `faqPageSchema` and `ServicePageTemplate` (Pitfall 1 in research).
- Do NOT add `localBusinessSchema()` — it's already in Footer.
- Do NOT modify `src/lib/schema.tsx` or `src/lib/metadata.ts` — they're already complete per research Goal 5 and 6.

**If `getFaqsByService("roofing")` returns fewer than 3 items at runtime**, that is a bug in Task 1 — fix Task 1, do not work around it here.
  </action>
  <verify>
```bash
# File exists at the correct static route
ls "src/app/(marketing)/services/roofing/page.tsx"

# All three JSON-LD blocks present
grep -c "JsonLd" "src/app/(marketing)/services/roofing/page.tsx"   # >= 3
grep "serviceSchema(service)" "src/app/(marketing)/services/roofing/page.tsx"
grep "breadcrumbSchema" "src/app/(marketing)/services/roofing/page.tsx"
grep "faqPageSchema(faqs)" "src/app/(marketing)/services/roofing/page.tsx"

# LocalBusiness schema must NOT be on service pages
! grep "localBusinessSchema" "src/app/(marketing)/services/roofing/page.tsx"

# buildMetadata with path and useAbsoluteTitle
grep 'path: "/services/roofing"' "src/app/(marketing)/services/roofing/page.tsx"
grep "useAbsoluteTitle: true" "src/app/(marketing)/services/roofing/page.tsx"

# ServicePageTemplate composition
grep "ServicePageTemplate" "src/app/(marketing)/services/roofing/page.tsx"

# Production build compiles the route
npx tsc --noEmit
npm run build 2>&1 | tail -40    # expect "Route (app) ... /services/roofing" in output

# Dev smoke test (optional, manual):
# npm run dev — then visit http://localhost:3000/services/roofing
# Expect: breadcrumbs, H1 "Roof Replacement & Repair in Omaha", problem checklist, process steps, CTA band, TrustStrip, before/after placeholders, testimonials, FAQ, related services, LeadForm with roofing pre-selected
```
  </verify>
  <done>
- `/services/roofing` route exists and builds successfully
- Page injects serviceSchema + breadcrumbSchema + faqPageSchema
- Page does NOT inject localBusinessSchema
- buildMetadata uses path "/services/roofing" and useAbsoluteTitle: true
- LeadForm on the rendered page has Service dropdown defaulted to roofing (verify by reading DOM in dev or by grep on ServicePageTemplate passing defaultService={service.slug})
- `npm run build` succeeds and includes the new route in the output
  </done>
</task>

</tasks>

<verification>
End of plan — run all of these before marking complete:

```bash
# 1. Type check
npx tsc --noEmit

# 2. Build (confirms route is generated)
npm run build

# 3. Content gap closures
grep -n "problemChecklist" src/content/services.ts
grep -c 'serviceSlug: "roofing"' src/content/faqs.ts           # >= 3
grep -c 'serviceSlug: "siding"' src/content/faqs.ts            # >= 3
grep -c 'serviceSlug: "gutters"' src/content/faqs.ts           # >= 3
grep -c 'serviceSlug: "emergency-tarping"' src/content/faqs.ts # >= 3
grep "defaultService" src/components/sections/LeadForm.tsx
grep "testimonials?" src/components/sections/TestimonialCarousel.tsx

# 4. Template structure
ls src/components/templates/ServicePageTemplate.tsx
ls src/components/sections/ServiceHero.tsx
ls src/components/sections/ServiceBreadcrumb.tsx
grep "isEmergency" src/components/templates/ServicePageTemplate.tsx

# 5. Roofing page
ls "src/app/(marketing)/services/roofing/page.tsx"
grep -c "JsonLd" "src/app/(marketing)/services/roofing/page.tsx"   # >= 3
grep 'path: "/services/roofing"' "src/app/(marketing)/services/roofing/page.tsx"

# 6. No anti-chaser contamination introduced anywhere
! grep -ri "limited time\|act now\|free roof\|insurance will pay\|zero out of pocket\|countdown" src/components/templates/ src/components/sections/ServiceHero.tsx src/components/sections/ServiceCTABand.tsx src/app/\(marketing\)/services/ src/content/faqs.ts

# 7. No LocalBusiness duplication on inner pages
! grep "localBusinessSchema" "src/app/(marketing)/services/roofing/page.tsx"
```
</verification>

<success_criteria>
- Visiting `/services/roofing` renders a fully populated service page with all 11 sections in Problem → Process → Proof → Form order
- Breadcrumbs visible (Home / Services / Roofing) AND BreadcrumbList JSON-LD shipped
- Service JSON-LD + FAQPage JSON-LD shipped; LocalBusiness NOT duplicated
- Unique title/description/canonical for /services/roofing via buildMetadata
- LeadForm on the page has Service dropdown defaulted to "roofing"
- Page internally links to the homepage (breadcrumb Home link + breadcrumb Services anchor) and to at least 2 sibling services (via RelatedServicesBlock)
- 3 visible `[PLACEHOLDER]` before/after gallery slots
- All content gaps (problemChecklist, per-service FAQs, LeadForm prop, TestimonialCarousel prop) closed so plans 04-02 and 04-03 can build pages without touching these files
- No anti-chaser forbidden strings introduced
- `npx tsc --noEmit` and `npm run build` both pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-service-pages/04-01-SUMMARY.md` summarizing:
- Gap closures (problemChecklist populated, N FAQs added per service, LeadForm + TestimonialCarousel prop additions)
- New components created (list with 1-line descriptions)
- /services/roofing route URL and verification checklist results
- Any placeholders or unknowns that wear `[PLACEHOLDER]` tags
- Anything discovered mid-execution that affects 04-02 or 04-03
</output>
