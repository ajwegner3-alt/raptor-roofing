---
phase: 03-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/Hero.tsx
  - public/images/hero-placeholder.jpg
  - public/images/HERO_CREDIT.txt
autonomous: true

must_haves:
  truths:
    - "A visitor opening the homepage sees a single full-viewport photograph of a roof/crew with a dark legibility overlay as the first impression"
    - "A visitor reads exactly one <h1> with a family-owned + responsiveness value prop headline and a supporting subheadline — no urgency copy"
    - "A visitor sees a micro-trust line directly beneath the headline with license, years in Omaha, and review rating (all flagged as placeholders where applicable)"
    - "A visitor can tap two side-by-side CTAs — a crimson click-to-call Call button and a slate-outline Request a Free Estimate button — that both meet 48x48px tap targets"
    - "Tapping Request a Free Estimate scrolls to the #estimate-form anchor (target will exist after 03-04; scroll behavior is correct regardless)"
    - "The hero photograph is a real JPG file served from /public/images/ (not an SVG, not a remote URL) and is rendered with next/image priority + fetchPriority='high' to protect LCP"
  artifacts:
    - path: "src/components/sections/Hero.tsx"
      provides: "Default-exported RSC Hero section with next/image background, dark overlay, h1, subheadline, micro-trust line, dual CTAs"
      min_lines: 60
    - path: "public/images/hero-placeholder.jpg"
      provides: "Real landscape photograph, >=1200px wide, committed to repo"
    - path: "public/images/HERO_CREDIT.txt"
      provides: "Attribution + source URL + Unsplash license note"
  key_links:
    - from: "src/components/sections/Hero.tsx"
      to: "public/images/hero-placeholder.jpg"
      via: "next/image with src='/images/hero-placeholder.jpg'"
      pattern: "src=\"/images/hero-placeholder\\.jpg\""
    - from: "src/components/sections/Hero.tsx"
      to: "#estimate-form"
      via: "next/link Link with href='#estimate-form'"
      pattern: "href=\"#estimate-form\""
    - from: "src/components/sections/Hero.tsx"
      to: "siteConfig.phone.href"
      via: "tel: anchor using @/content/site"
      pattern: "siteConfig\\.phone\\.href"
---

<objective>
Build the homepage Hero section — the first impression an Omaha homeowner sees when they land on the site — and seed the hero photograph asset so the component renders without a 404.

Purpose: The hero is where the anti-chaser story starts. A real photograph + family-owned headline + dual CTAs must load fast (LCP target) and do not include any urgency language. This plan is the foundation of Phase 3: until the hero exists and its image asset is in /public/images/, the homepage cannot boot.

Output: `src/components/sections/Hero.tsx` (RSC default export), `public/images/hero-placeholder.jpg` (real JPG downloaded from Unsplash), and `public/images/HERO_CREDIT.txt` (attribution).
</objective>

<execution_context>
@C:\Users\andre\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\andre\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-homepage/03-CONTEXT.md
@.planning/phases/03-homepage/03-RESEARCH.md
@src/content/site.ts
@src/app/globals.css
@src/app/(marketing)/layout.tsx
@src/components/layout/Header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download hero photograph to /public/images/hero-placeholder.jpg</name>
  <files>public/images/hero-placeholder.jpg, public/images/HERO_CREDIT.txt</files>
  <action>
The hero section MUST render a real landscape photograph. `next.config.ts` is empty and does not whitelist remote image hosts, so the file must live in `/public/images/`. The folder already exists (`.gitkeep` present) but has no image files.

STEP 1 — Download a Creative Commons / Unsplash-License photograph to `public/images/hero-placeholder.jpg`:

Primary candidate URL (Unsplash direct-download, license-clean, roofers on a suburban house):
```
https://images.unsplash.com/photo-1632759145355-8b8f0c6b7f3e?w=1920&q=80&fm=jpg&fit=crop
```

Run from project root (Git Bash):
```
curl -L -o public/images/hero-placeholder.jpg "https://images.unsplash.com/photo-1632759145355-8b8f0c6b7f3e?w=1920&q=80&fm=jpg&fit=crop"
```

If that specific photo ID 404s or returns < 50KB, fall back to one of these known-working Unsplash roofing/construction photos (try in order, stop at first success):
1. `https://images.unsplash.com/photo-1632759145355-92c48ba1f9df?w=1920&q=80&fm=jpg&fit=crop`
2. `https://images.unsplash.com/photo-1604709177225-055f99402ea3?w=1920&q=80&fm=jpg&fit=crop` (suburban house roof)
3. `https://images.unsplash.com/photo-1600585154340-be6161a56a0c?w=1920&q=80&fm=jpg&fit=crop` (modern house)
4. `https://images.unsplash.com/photo-1558618666-fcd25c85cd64?w=1920&q=80&fm=jpg&fit=crop` (residential exterior)

Accept ANY of these as long as the file is a real JPG photograph (not an SVG, not an HTML error page) and is >= 50KB.

STEP 2 — Verify the downloaded file:
```
file public/images/hero-placeholder.jpg     # must report JPEG image data
ls -la public/images/hero-placeholder.jpg   # must be >= 50000 bytes
```

If `file` reports anything other than JPEG image data (e.g. HTML, ASCII text), delete the file and retry with the next fallback URL. DO NOT ship a broken image.

STEP 3 — Write `public/images/HERO_CREDIT.txt` with exactly this content (replace {URL} with the URL that actually worked):
```
Raptor Roofing — hero-placeholder.jpg
Source: {URL}
License: Unsplash License (free for commercial use, no attribution required)
Status: PLACEHOLDER — Phase 8 handoff replaces with real Raptor crew photo
Downloaded: 2026-04-14
```

Do NOT use an SVG illustration. Do NOT skip this task. The Hero component in Task 2 references this exact path — if the file is missing, `next/image` returns 404 at runtime and the homepage LCP audit fails.
  </action>
  <verify>
  1. `file public/images/hero-placeholder.jpg` reports "JPEG image data"
  2. `ls -la public/images/hero-placeholder.jpg` shows size >= 50000 bytes
  3. `cat public/images/HERO_CREDIT.txt` contains the source URL and "Unsplash License"
  </verify>
  <done>A real JPG photograph exists at `public/images/hero-placeholder.jpg` (>= 50KB), and `public/images/HERO_CREDIT.txt` records its source + license.</done>
</task>

<task type="auto">
  <name>Task 2: Build Hero.tsx RSC section component</name>
  <files>src/components/sections/Hero.tsx</files>
  <action>
Create `src/components/sections/Hero.tsx` as a React Server Component (no `"use client"` directive). The directory `src/components/sections/` does NOT exist yet — create it as part of this task. Keep the file under 120 lines.

Default export:
```
export function Hero() { ... }
```
OR
```
export default function Hero() { ... }
```
— use a named export `export function Hero()` so `page.tsx` can import it via `import { Hero } from "@/components/sections/Hero"`. This matches the Phase 2 layout component import style.

STRUCTURE (in this exact order inside the returned JSX):

1. `<section>` wrapper with:
   - `className="relative isolate flex min-h-[calc(100dvh-4rem)] lg:min-h-[calc(100dvh-5rem)] items-center overflow-hidden"`
   - `aria-label="Raptor Roofing hero"`
   - The `4rem` / `5rem` subtract the sticky header height (`h-16` / `lg:h-20` in Header.tsx). Use `100dvh` not `100vh` to avoid iOS Safari address-bar overflow.

2. `<Image>` from `next/image` as the background photograph:
   - `src="/images/hero-placeholder.jpg"`
   - `alt="Raptor Roofing crew working on an Omaha home"`
   - `fill`
   - `priority`
   - `fetchPriority="high"` (must be on the Image — required for LCP)
   - `sizes="100vw"`
   - `className="absolute inset-0 -z-10 h-full w-full object-cover"`

3. Dark overlay sibling div:
   - `<div aria-hidden="true" className="absolute inset-0 -z-10 bg-[var(--color-overlay)]" />`
   - Use the `--color-overlay` CSS var already defined in globals.css (`rgba(13, 20, 23, 0.6)`). This ensures 4.5:1 contrast on white headline text (HME-03, A11Y-06).

4. Content container:
   - `<div className="relative mx-auto w-full max-w-4xl px-4 py-20 text-center sm:py-24 lg:px-8">`

5. Inside the content container, in order:

   a. `<h1>` — THE page's only H1:
      - `className="font-display text-4xl font-bold uppercase leading-tight tracking-tight text-white sm:text-5xl lg:text-6xl"`
      - Text: `The Family-Owned Roofer Omaha Calls Back.`
      (Exact copy per CONTEXT.md locked decision. Period included.)

   b. `<p>` subheadline:
      - `className="mx-auto mt-6 max-w-2xl font-body text-lg text-white/90 sm:text-xl"`
      - Text: `Fifteen years in the neighborhood. Same crew from estimate to final walkthrough. No subcontractors, no pressure, no disappearing act after the storm.`

   c. `<p>` micro-trust line:
      - `className="mt-6 font-display text-xs font-semibold uppercase tracking-widest text-white/80 sm:text-sm"`
      - Compose from siteConfig:
      ```
      Licensed NE #{license number placeholder text from siteConfig.license.number} · {siteConfig.founding.yearsInBusiness}+ Years in Omaha · {siteConfig.reviews.rating}★ {siteConfig.reviews.count} reviews [PLACEHOLDER]
      ```
      Note: `siteConfig.license.number` is the string `"// PLACEHOLDER: NE contractor license number — verify with Raptor"`. Rather than dumping that ugly string into the hero, render a cleaner static placeholder: `Licensed NE #[PLACEHOLDER]`. The reviews line uses real numeric values from siteConfig but the trailing `[PLACEHOLDER]` marker makes the FTC status clear.

      Simplest exact copy to render (use this literal string):
      ```
      Licensed NE #[PLACEHOLDER] · {siteConfig.founding.yearsInBusiness}+ Years in Omaha · {siteConfig.reviews.rating}★ {siteConfig.reviews.count} Reviews [PLACEHOLDER]
      ```
      Use `·` (middle dot, U+00B7) as separator — identical to Header/Footer style.

   d. CTA button row:
      - `<div className="mt-10 flex flex-col items-center justify-center gap-4 sm:flex-row">`
      - Inside:
        - Crimson filled call button (tel: anchor — external link, NOT next/link Link because it's not a route):
          ```
          <a
            href={siteConfig.phone.href}
            className="inline-flex min-h-[48px] min-w-[48px] items-center justify-center gap-2 rounded-md bg-accent-600 px-8 py-4 font-display text-base font-semibold uppercase tracking-wide text-white shadow-[var(--shadow-cta)] hover:bg-accent-700 transition-colors"
            aria-label={`Call Raptor Roofing at ${siteConfig.phone.display}`}
          >
            <Phone className="h-5 w-5" aria-hidden="true" />
            Call {siteConfig.phone.display}
          </a>
          ```
        - Slate-outline estimate button (next/link Link — same-page hash):
          ```
          <Link
            href="#estimate-form"
            className="inline-flex min-h-[48px] min-w-[48px] items-center justify-center rounded-md border-2 border-white bg-white/10 px-8 py-4 font-display text-base font-semibold uppercase tracking-wide text-white backdrop-blur-sm hover:bg-white hover:text-primary-700 transition-colors"
          >
            Request a Free Estimate
          </Link>
          ```

IMPORTS at top of file:
```
import Image from "next/image";
import Link from "next/link";
import { Phone } from "lucide-react";
import { siteConfig } from "@/content/site";
```

DO NOT:
- Add `"use client"` — this is an RSC.
- Use a `<a>` tag for the internal `#estimate-form` link (ESLint `@next/next/no-html-link-for-pages` forbids it on same-page navigation inside the App Router; use `next/link`).
- Add any urgency copy ("limited time", "act now", "24 hours only", "storm damage emergency").
- Use an SVG instead of a real photograph.
- Use `100vh` — iOS Safari overflow. Use `100dvh`.
- Forget `priority` or `fetchPriority="high"` on the Image — missing either tanks LCP.
- Add a carousel, video, or any motion. CONTEXT.md locks this to a single static photo.
- Add more than one `<h1>` — the hero owns the page's only H1.
- Import TrustStrip into Hero — TrustStrip is a separate section rendered by page.tsx.
- Include the emergency phone variant — use `siteConfig.phone.href` / `siteConfig.phone.display` (regular business phone).

After writing the file, run:
```
npx tsc --noEmit
npm run lint
```
Both must pass with zero errors on Hero.tsx before committing.
  </action>
  <verify>
  1. `npx tsc --noEmit` exits 0 (no TypeScript errors)
  2. `npm run lint` exits 0 for `src/components/sections/Hero.tsx` (no ESLint errors or warnings)
  3. `grep -l 'priority' src/components/sections/Hero.tsx` returns the file
  4. `grep -l 'fetchPriority="high"' src/components/sections/Hero.tsx` returns the file
  5. `grep -l 'href="#estimate-form"' src/components/sections/Hero.tsx` returns the file
  6. `grep -l 'siteConfig.phone.href' src/components/sections/Hero.tsx` returns the file
  7. File does NOT contain `"use client"` (grep returns nothing)
  8. File does NOT contain `100vh` (only `100dvh`)
  </verify>
  <done>`src/components/sections/Hero.tsx` exists as an RSC with next/image (priority + fetchPriority), dark overlay, single h1 with locked family-owned headline, subheadline, micro-trust line, dual CTAs (tel: anchor + Link to #estimate-form), passes tsc and eslint.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run lint` passes
- `public/images/hero-placeholder.jpg` exists, is a JPG, >= 50KB
- `public/images/HERO_CREDIT.txt` exists with Unsplash License notice
- `src/components/sections/Hero.tsx` exists and is a valid RSC module
- File grep confirms: `priority`, `fetchPriority="high"`, `siteConfig.phone.href`, `href="#estimate-form"`, `The Family-Owned Roofer Omaha Calls Back`
</verification>

<success_criteria>
- Hero.tsx is importable as `{ Hero }` from `@/components/sections/Hero`
- No TypeScript or ESLint errors
- Hero photograph is a real JPG, committed to the repo, referenced by exact path
- Hero component contains exactly one `<h1>`
- Both CTAs meet 48×48px tap target (inline `min-h-[48px]`)
- Dark overlay present; no urgency copy; no carousel; no client directive
- HME-01 (single static photo), HME-02 (locality headline, no urgency), HME-03 (dark overlay contrast), HME-04 (micro-trust line), HME-05 (exactly 2 primary CTAs) all partially satisfied — fully verifiable once 03-04 composes them into page.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage/03-01-SUMMARY.md` recording:
- Which Unsplash URL was successfully downloaded
- Exact file size of hero-placeholder.jpg
- Any deviations from the locked copy in CONTEXT.md (should be none)
- Confirmation that Hero.tsx is RSC (no "use client") and passes tsc + lint
</output>
