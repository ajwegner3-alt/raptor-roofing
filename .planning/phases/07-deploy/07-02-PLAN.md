---
phase: 07-deploy
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .planning/phases/07-deploy/07-RICH-RESULTS.md
autonomous: false

user_setup:
  - service: vercel-analytics
    why: "Enable free-tier Web Analytics for the raptor-roofing project (zero-config dashboard toggle; @vercel/analytics package + <Analytics /> component already wired in code)"
    dashboard_config:
      - task: "Enable Web Analytics"
        location: "Vercel Dashboard → raptor-roofing project → Analytics tab → Enable Web Analytics (free tier). Do NOT enable Speed Insights."
  - service: google-rich-results-test
    why: "Validate all injected schema (LocalBusiness, Service, FAQPage, BreadcrumbList) against the live preview URL"
    dashboard_config:
      - task: "Run Rich Results Test against 3 live URLs"
        location: "https://search.google.com/test/rich-results — paste URL → Test URL → record errors/warnings per page"

must_haves:
  truths:
    - "Vercel Web Analytics is toggled ON in the Vercel dashboard for the raptor-roofing project"
    - "https://raptor-roofing.vercel.app loads in incognito with no auth gate"
    - "Google Rich Results Test returns zero errors on the homepage"
    - "Google Rich Results Test returns zero errors on at least one /services/* page"
    - "Google Rich Results Test returns zero errors on at least one /service-areas/* page"
    - "The preview URL has NOT been submitted to Google Search Console (verified by absence of GSC submission action and absence of GSC code in repo)"
  artifacts:
    - path: ".planning/phases/07-deploy/07-RICH-RESULTS.md"
      provides: "Per-page Rich Results Test results with PASS/FAIL + error/warning counts"
      min_lines: 30
      contains: "Homepage"
  key_links:
    - from: "Vercel dashboard Analytics tab"
      to: "live raptor-roofing.vercel.app traffic stream"
      via: "manual toggle (one click)"
      pattern: "Web Analytics: Enabled"
    - from: "07-RICH-RESULTS.md"
      to: "live preview URL schema validation"
      via: "Google Rich Results Test against 3 URLs"
      pattern: "PASS|FAIL"
---

<objective>
Complete the deploy story by enabling the Vercel Analytics dashboard toggle, verifying the live preview URL is publicly accessible, validating all injected schema via Google Rich Results Test, and confirming the URL has NOT been submitted to Google Search Console.

Purpose: This closes DPL-06, DPL-07, and DPL-08 — the three remaining open deploy requirements. Analytics gives Andrew live traffic data when prospects open the URL. Rich Results Test proves the schema work from Phases 3-6 actually validates against Google. The negative GSC check honors CONTEXT.md's "do NOT submit" rule.

Output:
- Vercel Analytics toggled ON (manual)
- 07-RICH-RESULTS.md committed to phase dir with results from 3 live URLs
- Verification that no GSC submission has occurred and no GSC code exists in the repo
</objective>

<execution_context>
@C:\Users\andre\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\andre\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deploy/07-CONTEXT.md
@.planning/phases/07-deploy/07-RESEARCH.md
@.planning/phases/07-deploy/07-01-SUMMARY.md
@src/app/layout.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — verify live URL is up and Analytics code is wired</name>
  <files>(none — read-only verification)</files>
  <action>
Before asking Andrew to do anything in the dashboard, verify the live deploy from 07-01 finished successfully and that the Vercel Analytics code is already in place (per CONTEXT.md and 07-RESEARCH.md, package + component are pre-installed — only the dashboard toggle remains).

**Step 1: Confirm live URL responds**

```bash
curl -sI https://raptor-roofing.vercel.app | head -5
```
Expect: `HTTP/2 200`. If not 200, STOP and report — Andrew may need to check Vercel dashboard for a failed deploy from 07-01.

```bash
curl -sI https://raptor-roofing.vercel.app/sitemap.xml | head -5
curl -sI https://raptor-roofing.vercel.app/services/roofing | head -5
curl -sI https://raptor-roofing.vercel.app/service-areas/omaha | head -5
```
Expect: All 200. These are the 3 URL patterns we'll test in Task 3.

**Step 2: Confirm @vercel/analytics is installed and mounted**

Read the relevant lines (do NOT modify):

```bash
grep -n '"@vercel/analytics"' package.json
grep -n '@vercel/analytics' src/app/layout.tsx
grep -n '<Analytics' src/app/layout.tsx
```

Expect:
- package.json contains `"@vercel/analytics"` (any 2.x version)
- layout.tsx imports `Analytics` from `@vercel/analytics/react`
- layout.tsx mounts `<Analytics />` somewhere inside `<body>`

If any of these are missing, STOP — CONTEXT.md says they're pre-wired. Missing means a regression from a prior phase that needs investigation, not a Phase 7 fix.

**Step 3: Confirm zero GSC code in the repo (negative test for DPL-08)**

```bash
grep -rn "google-site-verification" src/ app/ 2>/dev/null
grep -rn "search.google.com/search-console" src/ app/ 2>/dev/null
grep -rn "googlebot" src/ app/ 2>/dev/null
```

Expect: ZERO matches in src/ or app/. The robots/sitemap files generated in Phase 6 may reference Google indirectly via the sitemap URL convention, but no GSC verification meta tag should be present anywhere.

If any matches are found, STOP and report — CONTEXT.md is explicit: do NOT submit to GSC. Any GSC code in the repo means a prior phase added it inadvertently and needs to be removed before pitch.
  </action>
  <verify>
- All three live curl checks return 200
- package.json + layout.tsx confirm Analytics is wired
- Zero GSC verification code in the source tree
  </verify>
  <done>
- Live URL is confirmed accessible
- Analytics code-side is confirmed wired (only dashboard toggle remains)
- DPL-08 (no GSC submission) is partially verified via the absence of GSC code; full verification comes after Task 4 confirms no submission action was taken
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Andrew enables Vercel Web Analytics in the dashboard</name>
  <what-built>
@vercel/analytics is installed in package.json and `<Analytics />` is mounted in src/app/layout.tsx. Code-side wiring is complete. Only the dashboard toggle remains, which has no CLI/API equivalent for the free Web Analytics tier — it must be clicked in the Vercel UI.
  </what-built>
  <how-to-verify>
**Manual checklist for Andrew (numbered per global CLAUDE.md rule):**

1. Open https://vercel.com/dashboard in your browser
2. Click into the **raptor-roofing** project
3. In the top nav, click the **Analytics** tab
4. You should see a panel titled "Web Analytics" with an **Enable** button. Click **Enable**
5. Confirm any "Free tier" / "Hobby" prompts — accept the free plan, do NOT upgrade
6. Do NOT enable **Speed Insights** (the tab next to Analytics) — this is a separate paid-only feature per CONTEXT.md decision
7. After enabling, you should see "Analytics is collecting data" or a placeholder dashboard with zero events (events take ~30 seconds to appear after the next page view)
8. Open https://raptor-roofing.vercel.app in a new incognito window to generate one test event
9. Wait ~60 seconds, then refresh the Analytics tab — you should see at least 1 visitor or 1 page view recorded

**Reply when complete:** Type "analytics enabled" or describe what you saw if it didn't work.
  </how-to-verify>
  <resume-signal>Type "analytics enabled" or describe issues</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Andrew runs Google Rich Results Test against 3 live URLs</name>
  <what-built>
Phases 3-6 injected JSON-LD schema across the site:
- Homepage: LocalBusiness + FAQPage + Service grid
- Service pages: Service schema + BreadcrumbList
- Service area pages: LocalBusiness reuse + BreadcrumbList

Rich Results Test must hit the live URL (not localhost) and is browser-only — no CLI exists for the public testing tool.
  </what-built>
  <how-to-verify>
**Manual checklist for Andrew (numbered per global CLAUDE.md rule):**

1. Open https://search.google.com/test/rich-results in your browser
2. **Test URL #1 (homepage — MANDATORY):**
   a. Paste `https://raptor-roofing.vercel.app` into the URL field
   b. Click **Test URL**
   c. Wait for results (~30 seconds)
   d. Record: number of detected items, error count, warning count, list of detected schema types (e.g., LocalBusiness, FAQPage)
3. **Test URL #2 (service page):**
   a. Paste `https://raptor-roofing.vercel.app/services/roofing`
   b. Click **Test URL**
   c. Record same metrics as above
4. **Test URL #3 (service area page):**
   a. Paste `https://raptor-roofing.vercel.app/service-areas/omaha`
   b. Click **Test URL**
   c. Record same metrics as above
5. For each result, take a screenshot of the "Detected items" panel (optional but helpful for debugging if errors appear)
6. **Reply with the data in this format** so the next task can write it to 07-RICH-RESULTS.md:

```
URL #1 Homepage:
  Detected items: <list>
  Errors: <count>
  Warnings: <count>
  Status: PASS / FAIL

URL #2 /services/roofing:
  Detected items: <list>
  Errors: <count>
  Warnings: <count>
  Status: PASS / FAIL

URL #3 /service-areas/omaha:
  Detected items: <list>
  Errors: <count>
  Warnings: <count>
  Status: PASS / FAIL
```

**Pass criterion (per CONTEXT.md):** Zero errors on homepage. Warnings are acceptable. Service + service-area URLs same standard (zero errors).

**If errors appear on any URL:** Capture the exact error message and which schema field it references. Phase 7 cannot ship until errors are zero. Andrew reports → planner reads result → either spawns a fix-it task inline or routes to gap-closure mode in a follow-up plan.
  </how-to-verify>
  <resume-signal>Paste the 3 results in the format above, or "all 3 pass" if zero errors and no warnings noted</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Write 07-RICH-RESULTS.md and verify GSC negative test</name>
  <files>.planning/phases/07-deploy/07-RICH-RESULTS.md</files>
  <action>
Take Andrew's reported results from Task 3 and persist them to the phase directory. Then explicitly document the DPL-08 negative test result.

**Step 1: Write 07-RICH-RESULTS.md**

Use this template, filling in the actual values from Task 3:

```markdown
# Phase 7 — Google Rich Results Test Results

**Date:** {today}
**Tested URL base:** https://raptor-roofing.vercel.app
**Tester:** Andrew (manual via https://search.google.com/test/rich-results)
**Pass criterion (per 07-CONTEXT.md):** Zero errors per page. Warnings acceptable.

---

## Homepage — `/`

**URL:** https://raptor-roofing.vercel.app
**Detected items:** {list from Andrew}
**Errors:** {count}
**Warnings:** {count}
**Status:** {PASS or FAIL}
**Notes:** {any error/warning details}

---

## Service Page — `/services/roofing`

**URL:** https://raptor-roofing.vercel.app/services/roofing
**Detected items:** {list}
**Errors:** {count}
**Warnings:** {count}
**Status:** {PASS or FAIL}
**Notes:** {details}

---

## Service Area Page — `/service-areas/omaha`

**URL:** https://raptor-roofing.vercel.app/service-areas/omaha
**Detected items:** {list}
**Errors:** {count}
**Warnings:** {count}
**Status:** {PASS or FAIL}
**Notes:** {details}

---

## Overall Result

**DPL-07 status:** {PASS if all 3 zero-error, FAIL otherwise}
**Pages skipped per CONTEXT.md:** /contact and /about (intentionally not tested in Phase 7)

**If FAIL:** Open follow-up plan to address errors before Phase 8.
```

**Step 2: Re-verify DPL-08 (no GSC submission)**

DPL-08 is a negative test — confirm absence of submission. There's no programmatic way to query "did Andrew submit this URL to GSC?" — but we can confirm:
1. No GSC verification meta tag in the codebase (re-run Task 1 Step 3 grep checks if needed)
2. Andrew has NOT been instructed by any task in this phase to submit to GSC
3. Confirm with Andrew via a brief in-chat question: "Confirm you have NOT submitted https://raptor-roofing.vercel.app to Google Search Console at any point. Reply 'confirmed not submitted' or describe."

Append the result of this confirmation to the bottom of 07-RICH-RESULTS.md under a new section:

```markdown
---

## DPL-08 — GSC Submission Negative Test

**Code-side:** Zero google-site-verification or GSC-related code in src/ or app/ (verified Task 1 Step 3)
**Action-side:** Andrew confirmed: {Andrew's reply}
**Status:** PASS — preview URL has not been submitted to Google Search Console
```

**Step 3: Commit the results doc**

```bash
git add .planning/phases/07-deploy/07-RICH-RESULTS.md
git commit -m "$(cat <<'EOF'
docs(07): capture Rich Results Test outcomes for live preview URL

Tested 3 URLs (homepage + 1 service + 1 service area) per CONTEXT.md.
DPL-07 and DPL-08 status documented.
EOF
)"
git push origin main
```

**Anti-patterns:**
- Do NOT submit the URL to Google Search Console under any circumstances (CONTEXT.md is explicit)
- Do NOT add a google-site-verification meta tag (same reason)
- Do NOT enable Speed Insights (CONTEXT.md decision)
- Do NOT add a custom domain (deferred per CONTEXT.md)
- If any URL FAILED with errors, do NOT mark Phase 7 complete — surface the errors and offer to spawn a gap-closure plan
  </action>
  <verify>
- 07-RICH-RESULTS.md exists in `.planning/phases/07-deploy/`
- File contains all 3 URLs with their PASS/FAIL status
- DPL-08 negative test section present with Andrew's confirmation
- Commit appears in `git log`
  </verify>
  <done>
- 07-RICH-RESULTS.md committed
- DPL-06 verified (Andrew confirmed Analytics is collecting events in Task 2)
- DPL-07 verified (3 URLs tested, results documented)
- DPL-08 verified (negative test passed code-side + action-side)
- All 8 deploy requirements (DPL-01 through DPL-08) have a documented PASS or N/A status
  </done>
</task>

</tasks>

<verification>
- Vercel Web Analytics dashboard shows "Enabled" and at least 1 collected event from Andrew's incognito visit
- 07-RICH-RESULTS.md captures all 3 URL test results with zero errors on each
- No google-site-verification meta tag exists in the repo
- Andrew confirmed no GSC submission was made
- All assertions in 07-CONTEXT.md "Decisions" section are honored: zero env vars, option-(a) form stub, free Analytics tier, no Speed Insights, no GSC, no custom domain
</verification>

<success_criteria>
- DPL-06: Vercel Analytics enabled — PASS
- DPL-07: Rich Results Test passes with zero errors on 3 URLs — PASS
- DPL-08: Preview URL NOT submitted to GSC — PASS
- Phase 7 deploy story is complete; Phase 8 (Manual QA + Handoff) can begin
</success_criteria>

<output>
After completion, create `.planning/phases/07-deploy/07-02-SUMMARY.md` per the summary template. Include:
- DPL-01 through DPL-08 status table
- Reference to 07-RICH-RESULTS.md
- Vercel Analytics enabled confirmation
- Live URL final-state spot-check (curl 200 on homepage + sitemap)
- Status: Phase 7 ready to mark complete in ROADMAP.md
</output>
