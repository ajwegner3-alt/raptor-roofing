---
phase: 05-about-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/about/page.tsx
  - src/components/sections/AboutHero.tsx
autonomous: true

must_haves:
  truths:
    - "/about renders the chronological family-owned story (1–1.5 screens)"
    - "/about has a standalone 'Every Crew Is Raptor' no-subcontractors section"
    - "/about has an educational anti-chaser section (no naming, no 'chaser' word)"
    - "/about reuses the sitewide TrustStrip and TestimonialCarousel"
    - "/about ends with a CTA block pointing to /contact and tel: link (NO embedded form)"
    - "/about emits BreadcrumbList JSON-LD only (no duplicate LocalBusiness)"
    - "/about has unique title/description via buildMetadata({ path: '/about', useAbsoluteTitle: true })"
  artifacts:
    - path: "src/app/(marketing)/about/page.tsx"
      provides: "About page Server Component, metadata export, BreadcrumbList JSON-LD"
      exports: ["default", "metadata"]
      min_lines: 120
    - path: "src/components/sections/AboutHero.tsx"
      provides: "About page hero with 5-layer visual stack (background photo + gradient overlay + noise + content + SVG divider), H1, micro-trust line, single Call CTA"
      exports: ["AboutHero"]
      min_lines: 80
  key_links:
    - from: "src/app/(marketing)/about/page.tsx"
      to: "src/components/sections/AboutHero.tsx"
      via: "import + JSX usage"
      pattern: "import.*AboutHero.*from.*AboutHero"
    - from: "src/app/(marketing)/about/page.tsx"
      to: "src/lib/metadata.ts"
      via: "buildMetadata() call"
      pattern: "buildMetadata\\("
    - from: "src/app/(marketing)/about/page.tsx"
      to: "src/lib/schema.tsx"
      via: "breadcrumbSchema() call"
      pattern: "breadcrumbSchema\\("
    - from: "src/app/(marketing)/about/page.tsx"
      to: "src/components/sections/TestimonialCarousel.tsx"
      via: "import + JSX usage"
      pattern: "TestimonialCarousel"
---

<objective>
Build the /about page as a short (1–1.5 screen) chronological family narrative that supports the anti-chaser positioning without being preachy or naming competitors. The page reuses sitewide trust components and ends with a CTA pointing to /contact (NO embedded form).

Purpose: Gives the pitch demo a credible "who we are" story that reinforces "Every Crew Is Raptor" (no subs) and educates homeowners about post-storm risks without using the word "chaser."

Output: A new About page Server Component, a new AboutHero component (NOT a reuse of ServiceHero — see action notes), and BreadcrumbList JSON-LD only (no duplicate LocalBusiness — Footer already owns it).
</objective>

<execution_context>
@C:\Users\andre\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\andre\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/05-about-contact/05-CONTEXT.md
@.planning/phases/05-about-contact/05-RESEARCH.md
@.planning/REQUIREMENTS.md
@src/components/sections/ServiceHero.tsx
@src/components/sections/TestimonialCarousel.tsx
@src/components/sections/WhyNotChaser.tsx
@src/lib/metadata.ts
@src/lib/schema.tsx
@src/content/site.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AboutHero component</name>
  <files>src/components/sections/AboutHero.tsx</files>
  <action>
Create a NEW Server Component `AboutHero` at `src/components/sections/AboutHero.tsx`. Do NOT reuse ServiceHero — ServiceHero requires a Service prop and always renders LeadForm in the right column, neither of which fits the About page (per CONTEXT.md: "No lead form on About page").

The component must replicate ServiceHero's 5-layer visual stack so it feels native to the site:
1. Background image layer (use a roofing/family-ish placeholder — point to `/images/about-hero.webp` and add a TODO comment that Phase 8 swaps the asset)
2. Dark gradient overlay (reuse the same gradient classes ServiceHero uses — read ServiceHero.tsx and copy the overlay constants verbatim)
3. Subtle noise texture layer (same as ServiceHero)
4. Foreground content column (single column, centered or left-aligned — NOT the two-column hero+form split that ServiceHero uses)
5. SVG bottom divider (reuse the same SVG curve/diagonal ServiceHero uses for visual continuity)

Foreground content for the About hero:
- Eyebrow line: "About Raptor Roofing"
- H1: Something like "Built on a Family Name. Backed by Our Own Crews." (Claude's discretion — must NOT use the word "chaser" and must NOT match any other H1 on the site)
- Subheadline (1–2 sentences): Family-owned framing, connects to Omaha
- Micro-trust line directly below the subheadline: "Family-Owned Since [PLACEHOLDER: founding year] · No Subcontractors · Locally Owned in Omaha" — render the [PLACEHOLDER: founding year] tag literally as visible text so Phase 8 handoff can find and swap it
- Single primary CTA: a `tel:` button to `siteConfig.phone` ("Call (402) 885-1462") — NO "Free Estimate" button (the end-CTA block at the bottom of the page handles that)

DO NOT include a LeadForm. DO NOT take a `service` prop. The component is a Server Component (no "use client"). Props interface should be empty or accept only optional override strings (eyebrow/title/subtitle) — keep it simple.

Read `src/components/sections/ServiceHero.tsx` first to extract the exact overlay/noise/divider class strings and asset paths so AboutHero stays visually consistent.
  </action>
  <verify>
- File exists at `src/components/sections/AboutHero.tsx`
- Component is a Server Component (no `"use client"` directive at top)
- No import of `LeadForm` anywhere in the file
- File contains `[PLACEHOLDER: founding year]` literal string
- File contains a `tel:` link to the site phone number
- `npm run build` succeeds (component compiles, no type errors)
  </verify>
  <done>
AboutHero renders independently of any service data and visually matches the ServiceHero aesthetic (same overlay, noise, divider) while presenting a single-column About-specific message with a Call CTA and no embedded form.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build /about page Server Component</name>
  <files>src/app/(marketing)/about/page.tsx</files>
  <action>
Create the About page at `src/app/(marketing)/about/page.tsx` as a Server Component composing:

1. **`<AboutHero />`** — the new component from Task 1
2. **Chronological story section** — Header H2 like "Our Story" or "How Raptor Started". Body: 2–3 short paragraphs telling a chronological family narrative starting "Founded in [PLACEHOLDER: founding year] by [PLACEHOLDER: founder name], Raptor Roofing began as a small family operation…" — keep all unknown facts (founder, year, family size, license number) wrapped in `[PLACEHOLDER: ...]` literal tags so Phase 8 handoff can grep and swap. Total length: ~150–250 words. Do NOT use stock startup language ("we set out to disrupt") — keep it grounded, plainspoken, blue-collar.

3. **"Every Crew Is Raptor" section** — Standalone H2 titled exactly "Every Crew Is Raptor" (or visually equivalent). 3–4 short paragraphs explaining what no-subcontractors means in practice and why it matters to homeowners (accountability, training, quality control, the same crew on Day 1 is the same crew on Day 5). NOT a bullet list, NOT a comparison table — paragraph form per CONTEXT.md.

4. **Educational anti-chaser section** — H2 something like "What to Watch For After a Storm" (Claude's discretion — must NOT contain the word "chaser"). 3–5 plain-language educational tips homeowners can use to spot fly-by-night operators (red flags: door knocking the day after a storm, no local address, asking for full payment up front, pushing you to sign on the spot, no physical office, can't produce a Nebraska contractor license number). Frame as homeowner education, NOT as "we don't do this." Do NOT name competitors. Do NOT use the word "chaser" anywhere visible to the reader.

5. **TrustStrip** — Reuse the existing sitewide TrustStrip component (find it under `src/components/sections/` or `src/components/global/` — likely already imported in the homepage and ServiceHero). Pass any required props the same way the homepage does.

6. **TestimonialCarousel slice** — Reuse `<TestimonialCarousel />` from `src/components/sections/TestimonialCarousel.tsx`. Keep the [PLACEHOLDER] banner the homepage version already shows.

7. **End CTA block** — A simple closing section with a heading like "Talk to the Crew Behind the Name", body line, and TWO CTAs side-by-side: a `tel:` button (Call (402) 885-1462) and a "Get a Free Estimate" link to `/contact` (NOT an embedded form — just a Link to the contact page).

**Metadata export** (top of file, after imports):
```ts
export const metadata = buildMetadata({
  path: '/about',
  title: 'About Raptor Roofing — Family-Owned, Omaha-Based, No Subcontractors',
  description: 'Meet the family behind Raptor Roofing. Every crew on your property works for us — no subcontractors, no chasers, just Omaha\'s locally-owned roofer.',
  useAbsoluteTitle: true,
});
```
(Title/description copy is Claude's discretion as long as it is unique to /about, under 60 chars title, ~150 chars description, and contains the primary keyword + locality per the SEO requirements in project CLAUDE.md.)

**JSON-LD**: Render BreadcrumbList JSON-LD via the existing `breadcrumbSchema()` factory in `src/lib/schema.tsx`. Items: `[{ name: "Home", url: "/" }, { name: "About", url: "/about" }]`. Use the existing `<JsonLd>` rendering pattern other pages in the codebase use (read another page like `src/app/(marketing)/services/[slug]/page.tsx` for the exact pattern).

**DO NOT emit a LocalBusiness JSON-LD on the About page.** The Footer already renders LocalBusiness on every page via the marketing layout. Per CONTEXT.md and Pitfall 4 in RESEARCH.md, duplicating LocalBusiness creates structured-data confusion. The Phase 5 ABT-03 requirement is satisfied transitively by the Footer schema. Add a code comment in the page noting this:
```tsx
// LocalBusiness JSON-LD is rendered globally by Footer — do NOT duplicate here.
// See .planning/phases/05-about-contact/05-CONTEXT.md and 05-RESEARCH.md Pitfall 4.
```

Page must be mobile-first, use existing Tailwind theme tokens (no arbitrary values), and stay under ~1.5 screens scroll on a typical desktop viewport.
  </action>
  <verify>
- File exists at `src/app/(marketing)/about/page.tsx`
- File exports `default` (the page) and `metadata`
- `grep -n "buildMetadata" src/app/(marketing)/about/page.tsx` matches
- `grep -n "breadcrumbSchema" src/app/(marketing)/about/page.tsx` matches
- `grep -n "localBusinessSchema\|LocalBusiness" src/app/(marketing)/about/page.tsx` returns ZERO matches (must NOT duplicate)
- `grep -n "chaser" src/app/(marketing)/about/page.tsx` returns ZERO matches (educational section must not use the word)
- `grep -n "LeadForm\|ContactForm" src/app/(marketing)/about/page.tsx` returns ZERO matches (no embedded form)
- `grep -n "Every Crew Is Raptor\|Every crew" src/app/(marketing)/about/page.tsx` matches
- `grep -n "PLACEHOLDER" src/app/(marketing)/about/page.tsx` matches at least once (founding year, founder name)
- `npm run build` succeeds
- `npm run dev` then visit http://localhost:3000/about — page renders end-to-end with hero, story, every-crew section, anti-chaser section, trust strip, testimonials, end CTA
  </verify>
  <done>
/about renders cleanly, contains every required section in order, ships BreadcrumbList JSON-LD only, does NOT duplicate LocalBusiness, has unique metadata, contains [PLACEHOLDER] tags for unknown facts, and the end CTA links to /contact and tel:.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run from project root:

```bash
npm run build
```

Build must succeed with zero errors. Then:

```bash
npm run dev
```

Visit:
- http://localhost:3000/about — verify hero, story, every-crew section, anti-chaser section, trust strip, testimonials, end CTA all render
- View page source — confirm BreadcrumbList JSON-LD present, LocalBusiness JSON-LD ONLY in the Footer (single instance, not duplicated)
- View page source — confirm `<title>` is unique to /about (different from / and /services/*)

Hard-gate greps:
- `grep -rn "localBusinessSchema" src/app/(marketing)/about/` → zero matches
- `grep -n "chaser" src/app/(marketing)/about/page.tsx` → zero matches
- `grep -n "LeadForm" src/app/(marketing)/about/page.tsx` → zero matches
- `grep -n "PLACEHOLDER" src/app/(marketing)/about/page.tsx` → at least one match
</verification>

<success_criteria>
- /about page exists and renders all 7 required sections in order (hero, story, every-crew, anti-chaser, trust, testimonials, end CTA)
- Page emits BreadcrumbList JSON-LD only — no LocalBusiness duplication
- buildMetadata() produces unique title/description for /about
- AboutHero is a NEW Server Component (not a ServiceHero reuse)
- No embedded LeadForm or ContactForm on /about
- All unknown facts (founder, year) wrapped in [PLACEHOLDER: ...] literal tags
- Anti-chaser section is educational and never uses the word "chaser"
- ABT-01, ABT-02, ABT-03 (via Footer schema), ABT-04 marked as covered
</success_criteria>

<output>
After completion, create `.planning/phases/05-about-contact/05-01-SUMMARY.md` describing what was built, decisions made, and any [PLACEHOLDER] tags Phase 8 must swap.
</output>
